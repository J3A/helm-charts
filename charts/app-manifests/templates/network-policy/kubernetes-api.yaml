{{- range .Values.networkPolicy.kubernetesApi  -}}
apiVersion: "cilium.io/v2"
kind: CiliumNetworkPolicy
metadata:
  name: {{ include "app-manifests.name" $ }}-{{ .name }}-api-egress
  labels:
    {{- include "app-manifests.labels" $ | nindent 4 }}
spec:
  description: Policy to allow egress to kube-apiserver
  endpointSelector:
    matchLabels:
      {{- toYaml .matchLabels | nindent 6 }}
  egress:
    - toEntities:
      - kube-apiserver
{{- end -}}